import { NgModule } from '@angular/core';
import { ConfigLeaflet } from './config/config';

@NgModule({
    declarations: [
    ],
    providers: [
      ConfigLeaflet,
    ],
})

export class SharedModule {
}